version: '3.8'

services:
  postgres:
    image: postgres:latest
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: renting_app
      POSTGRES_DB: renting_app
      POSTGRES_PASSWORD: renting_app